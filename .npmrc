# Keep installs dependency-light and free of native/compiled addons - this
# app is designed to run on constrained hosts too (e.g. a phone under
# Termux), where a C build toolchain may not be present.
omit=optional
